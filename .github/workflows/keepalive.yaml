# https://share.google/aimode/WtAUTFLxlLqbopKoh
name: Keep Workflows Alive

on:
  schedule:
    # Runs at 01:03 on day 1 of every month
    - cron: "3 1 1 * *"

jobs:
    workflow-keepalive:
        if: github.event_name == 'schedule'
        runs-on: ubuntu-latest
        permissions:
          actions: write
        steps:
          - uses: liskin/gh-workflow-keepalive@v1
